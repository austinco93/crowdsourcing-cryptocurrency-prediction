<?xml version="1.0" encoding="utf-8"?>
<manifest package="com.bubble.crowdsourcingcryptocurrencyprediction"
          xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".HomeActivity"
            android:label="Home"/>
        <activity
            android:name=".admin.AdminActivity"
            android:label="Admin"/>
        <activity
            android:name=".SplashActivity"
            android:label="Login/Register">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <activity android:name=".PredictionActivity"/>
        <activity android:name=".admin.CreatePredictionEntryActivity"/>
        <activity android:name=".PredictionListActivity">
        </activity>
    </application>

</manifest>